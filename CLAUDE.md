## WritBase Task Context

WritBase tasks track work items for this project. When the conversation naturally involves
creating, completing, or blocking work items, the `add_task` and `update_task` MCP tools
are available. Use `/extract-tasks` at the end of a session to review untracked items.
